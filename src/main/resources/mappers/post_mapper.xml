<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.koreait.dbms_study.mapper.PostMapper">

    <insert id="insertPost" parameterType="com.koreait.dbms_study.entity.Post">
        INSERT INTO post_tb (title, content, user_id, create_dt)
        VALUES (#{title}, #{content}, #{userId}, NOW())
    </insert>

    <select id="selectPostById" resultType="com.koreait.dbms_study.entity.Post">
        SELECT * FROM post_tb WHERE post_id = #{postId}
    </select>

    <select id="selectAllPosts" resultType="com.koreait.dbms_study.entity.Post">
        SELECT * FROM post_tb ORDER BY post_id DESC
    </select>

    <update id="updatePost" parameterType="com.koreait.dbms_study.entity.Post">
        UPDATE post_tb
        SET title = #{title}, content = #{content}, update_dt = NOW()
        WHERE post_id = #{postId}
    </update>

    <delete id="deletePost" parameterType="int">
        DELETE FROM post_tb WHERE post_id = #{postId}
    </delete>
</mapper>